#include <iostream>
using namespace std;

//there are two header files 1.system header files 2. user defined header files
int main(){
    cout<<"Operator in cpp"<<endl;
    //Arithmetic operator(-,+,/,*,//,++,--,)
    //Assingment operator (used assighn value to variable
    // Comparision operator (<,>,<<,>>,>=,<=,==)
    //logical operater (&& || !)
    
}